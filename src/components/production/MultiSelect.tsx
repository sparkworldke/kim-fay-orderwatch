/**
 * @deprecated Import from `@/components/ui/multi-select` instead.
 * Kept as a re-export so existing production imports keep working.
 */
export {
  MultiSelect,
  type MultiSelectOption,
  type MultiSelectProps,
} from "@/components/ui/multi-select";
